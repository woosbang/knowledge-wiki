# Knowledge Wiki — PRD

작성일: 2026-09-23

---

## 1. 문제

공부한 내용을 HTML 로 정리해 `docs/` 에 모으고 있다. 파일이 늘어나면 곧 이런 상태가 된다.

- 한 폴더에 주제가 뒤섞여 무엇이 어디 있는지 모른다
- 같은 주제를 여러 번 정리해 중복이 생긴다 (이미 `git-*` 문서가 4개, RAG 가이드가 2개 있다)
- 오래된 내용이 최신 내용과 구분되지 않는다
- 파일 하나하나는 잘 만들었지만, 모아놓고 보면 '지식베이스'가 아니라 '폴더'다

수작업으로 목차를 관리하면 결국 관리가 밀린다. 정리 비용이 정리 효용을 넘는 순간 멈춘다.

## 2. 목표

**`docs/` 에 HTML 을 넣는 것 외에 사용자가 해야 할 일이 없어야 한다.**

그 뒤의 분류 · 중복 탐지 · 목차 구성 · 배포는 자동으로 일어난다.

### 성공 기준

| 기준 | 측정 |
|---|---|
| 투입 비용 | 문서 1개 추가에 사용자가 쓰는 시간 < 1분 (파일 복사 + `/wiki-sync` 실행) |
| 검색성 | 문서 50개 시점에도 원하는 내용을 3클릭 또는 1검색 안에 찾는다 |
| 중복 관리 | 겹치는 문서가 생기면 다음 sync 에서 반드시 리포트에 뜬다 |
| 내용 보존 | 원본 HTML 의 내용이 위키에서 유실되지 않는다 (0건) |
| 배포 | main 에 push 하면 사람 개입 없이 사이트가 갱신된다 |

### 비목표 (하지 않는 것)

- 원본 HTML 을 자동으로 수정하거나 병합하는 것 — 사용자의 자산이므로 건드리지 않는다
- 위키 안에서 문서를 편집하는 CMS — 편집은 원본 HTML 에서 한다
- 전문 검색 서버 · 데이터베이스 — 정적 파일과 클라이언트 검색으로 충분하다 (문서 수백 개까지)
- 다중 사용자 · 권한 관리 — 개인 지식베이스다

## 3. 설계 결정과 근거

### 3.1 원본은 불변, 메타데이터는 별도

`docs/` 는 읽기 전용 입력이고, AI 의 판단은 전부 `wiki/entries/<slug>.json` 에 쌓인다.

- 원본을 건드리지 않으므로 AI 가 잘못 판단해도 잃는 것이 없다
- 메타데이터가 사라져도 `uv run wiki scan` 으로 골격은 복원된다
- 사람이 AI 의 판단을 언제든 손으로 덮어쓸 수 있다 (그냥 JSON 을 고치면 된다)

### 3.2 본문을 추출해 통일 템플릿으로 재렌더링 — 단, 원본도 함께 배포

각 원본은 자체 CSS·폰트·테마를 갖고 있어 그대로 모으면 사이트가 누더기가 된다.
그래서 본문만 뽑아 하나의 테마로 다시 그린다.

**추출은 완벽할 수 없다.** 그래서 원본 파일을 `site/raw/<slug>.html` 로 그대로 함께 배포하고,
모든 문서 페이지 하단과 사이드바에 **'원본 그대로 보기'** 링크를 둔다.
추출이 깨져도 내용은 반드시 남는다 — 이것이 "내용 보존 0건 유실" 기준을 지키는 장치다.

추출기가 보존하는 것: 제목 구조, 문단, 목록, 표, 코드블록, 링크, **SVG 다이어그램**,
그리고 원본의 콜아웃 박스(→ `blockquote`)와 배지/라벨(→ `p.kicker`).

### 3.3 카테고리 체계는 사람이 소유한다

`wiki/taxonomy.yaml` 이 분류의 유일한 기준이고, AI 는 **그 안에서만** 분류한다.

AI 가 자유롭게 카테고리를 만들게 두면, 문서가 늘수록 비슷비슷한 카테고리가 번식해서
결국 처음의 '폴더 하나에 다 섞인 상태'로 돌아간다. 새 카테고리가 필요하면
AI 는 `wiki/reports/taxonomy-proposals.md` 에 제안하고, 승인은 사람이 한다.

맞는 자리가 없는 문서는 `misc` 로 가고, `misc` 가 3개를 넘으면 리포트가 신설을 제안한다.

### 3.4 중복은 '탐지'하되 '해결'하지 않는다

문자 n-gram 유사도로 후보를 먼저 거르고(기계), 실제 판정은 AI 가 하고(판단),
최종 조치는 메타데이터로만 표현한다:

- 상위 호환 관계 → `status: superseded` + `superseded_by`
- 보완 관계 → 양쪽 `related`
- 애매함 → `overlaps` 에 기록하고 리포트에서 사람이 본다

문서를 삭제하거나 병합하는 자동 동작은 넣지 않는다. 되돌리기 어려운 동작이기 때문이다.

### 3.5 AI 실행은 두 경로, 같은 지침

`pipeline/prompts/classify.md` 하나를 **Claude Code 슬래시 커맨드와 API 스크립트가 공유한다.**
어느 쪽으로 돌려도 결과 스키마와 판단 기준이 같다. 기준을 바꾸려면 그 파일만 고친다.

| 경로 | 언제 쓰나 | 비용 |
|---|---|---|
| `/wiki-sync` (Claude Code) | 평소. 결과를 눈으로 보고 바로 고칠 수 있다 | 구독에 포함 |
| `uv run wiki classify` (API) | GitHub Actions 무인 실행 | API 토큰 과금 |

Phase 1 은 슬래시 커맨드만으로 운영하며 분류 품질을 확인한다.
품질이 안정되면 Phase 3 에서 API 경로를 Actions 에 붙인다.

### 3.6 재분류는 해시로 판단한다

entry 의 `classifier_hash` 가 현재 `source_hash` 와 다르면 원본이 바뀐 것이므로 재분류 대상이 된다.
분류 결과가 최신이면 AI 를 호출하지 않는다 — 문서 100개를 매번 다시 분류하는 낭비를 막는다.

## 4. 데이터 흐름

```
docs/*.html
    │  (읽기 전용)
    ▼
wiki scan ──────────► wiki/extracted/<slug>.json   본문 추출 (기계)
    │                 wiki/entries/<slug>.json     골격 + source_hash
    ▼
AI 분류 ────────────► wiki/entries/<slug>.json     category/tags/summary/related/…
 (/wiki-sync                                        + classifier_hash
  또는 wiki classify)
    ▼
wiki audit ─────────► wiki/reports/health.md       중복·노후·미분류 리포트
    ▼
wiki build ─────────► site/                        정적 위키
    │                   ├ index.html               카테고리 그리드 + 최근 문서
    │                   ├ c/<category>.html        카테고리별 목록
    │                   ├ d/<slug>.html            통일 템플릿 문서
    │                   ├ raw/<slug>.html          ★ 원본 그대로
    │                   └ search-index.json        클라이언트 검색
    ▼
GitHub Actions ─────► GitHub Pages
```

## 5. 범위

### Phase 1 — 기반 (완료)

- [x] 폴더 구조와 소유권 정의
- [x] HTML 추출기 (SVG·표·코드블록·콜아웃 보존)
- [x] entry 스키마와 해시 기반 변경 감지
- [x] 카테고리 체계 (`taxonomy.yaml`)
- [x] 정적 사이트 빌더 (홈 / 카테고리 / 문서 / 원본)
- [x] 클라이언트 검색 (외부 의존성 없음)
- [x] 중복·노후 리포트 (`wiki audit`)
- [x] `/wiki-sync`, `/wiki-audit` 슬래시 커맨드

### Phase 2 — 배포 (완료, 2026-09-24)

- [x] git 저장소 초기화, GitHub 저장소 생성 — `woosbang/knowledge-wiki`
- [x] GitHub Actions 로 Pages 자동 배포 — https://woosbang.github.io/knowledge-wiki/
- [x] 사이트 내부 링크는 상대 경로이므로 배포 경로에 따른 설정 변경이 없다
- [x] 공개/비공개 결정 — **공개 저장소**로 결정. 사내 문서 기반 자료 2건은 공개 전에 `docs/` 에서 삭제했고
      git 이력에도 남기지 않았다. **앞으로 `docs/` 에 넣는 파일은 공개된다는 전제로 검토할 것.**
      배포 서버는 필요해지면 나중에 옮긴다.

### Phase 3 — 무인화 (선택)

- [ ] `ANTHROPIC_API_KEY` 를 저장소 시크릿으로 등록
- [ ] push 시 Actions 가 `wiki classify` → `build` → 배포까지 자동 수행
- [ ] 월 1회 `wiki audit` 정기 실행 및 리포트 이슈 생성

### Phase 5 — 정돈된 위키 본문 (8절 참조. 5a·5b 완료 2026-09-24)

- [x] 5a 토픽 데이터 모델 + 합성 지침 + `/wiki-sync` 3·4단계 + 토픽 페이지 빌드 — 토픽 7개(정돈본 3 + 원본 그대로 4)로 검증
- [x] 5b 인용·코드·분량 검사(`synth-done`, audit), `locked`/memo 보존, 토픽 우선 검색
- [ ] 5c API 경로 `wiki synthesize` — 코드는 있으나 **실제 API 호출로 검증하지 않음**. Phase 3 와 함께 `--dry-run` 으로 먼저 확인

### Phase 4 — 확장 (아이디어)

- [ ] 태그 페이지 (`/tag/<tag>.html`) — `t/` 는 토픽 페이지가 쓴다
- [ ] 문서 간 관계 그래프 시각화
- [ ] 문서 내 앵커 단위 검색 결과
- [ ] 읽은 기록 / 복습 주기 표시

## 6. 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 추출이 원본 내용을 깨뜨린다 | 내용 유실 | 원본을 `site/raw/` 로 함께 배포 + 모든 페이지에 링크 |
| AI 분류가 들쭉날쭉하다 | 카테고리 혼란 | 지침 파일 1개로 통일, `taxonomy.yaml` 로 선택지 제한, 결과를 JSON 으로 남겨 사람이 수정 가능 |
| 카테고리가 무한 증식 | 체계 붕괴 | AI 는 제안만, 승인은 사람 |
| 사내 문서 기반 자료의 공개 노출 | 보안 사고 | 저장소와 Pages 가 **공개**이므로 `docs/` 에 넣기 전에 내용을 반드시 검토. 실수로 넣었다면 파일 삭제만으로는 부족하고 git 이력도 정리해야 한다 |
| 문서가 수백 개로 늘어 검색 인덱스가 비대 | 초기 로딩 지연 | 본문을 4,000자로 잘라 색인. 넘어가면 인덱스 분할 또는 Pagefind 도입 |
| 원본 파일명 변경 → slug 변경 → 링크 깨짐 | 외부 링크 무효 | 파일명을 바꾸지 않는 것을 권장. 바꿨다면 audit 의 '고아 entry'로 잡힌다 |

## 7. 열려 있는 결정

- **저장소 공개 여부** — ~~private 권장~~ → **2026-09-24 공개로 결정.** 사내 문서 기반 자료는 삭제했다. 배포 서버 이전은 필요해지면 검토한다.
- **한글 slug 유지 여부** — 현재는 가독성을 위해 한글을 slug 에 살린다(`uv-환경-guidebook`). URL 은 퍼센트 인코딩되지만 동작에는 문제가 없다. 외부 공유가 잦아지면 영문 slug 로 전환을 검토한다.

---

## 8. 정돈된 위키 본문 — 토픽 페이지 설계 (Phase 5 제안)

작성일: 2026-09-24 · 상태: **8.13 의 세 결정 모두 승인 → 5a·5b 구현 완료** (같은 날)

### 8.1 왜 필요한가

지금 위키는 원본 문서를 **한 편씩 그대로** 보여준다. 중복은 `related`·`overlaps`·`superseded_by` 로
관계만 표시하고, 내용이 겹치거나 서로 어긋나는 문제는 독자가 두 문서를 오가며 스스로 풀어야 한다.

CLAUDE.md 절대 규칙 1에 새로 붙은 요구는 다르다:

> 각 원본 문서간에는 중복된 내용도 있을 수 있고, 수정이 필요한 내용이 있을 수도 있다.
> wiki 문서에는 이런 문제가 해결되어 정돈된 최신 정보로 표시되어야 한다.

즉 위키는 "원본 모음"이 아니라 **"주제별로 정리된 최신 지식"** 이어야 한다.
다만 규칙 1의 앞부분(원본 불변)과 규칙 5(문서를 병합·은닉하지 않음)는 그대로다.
세 요구를 동시에 만족하는 구조가 필요하다.

### 8.2 핵심 결정: 원본 층 위에 토픽 층을 얹는다

| 층 | 단위 | 누가 쓰나 | 위키에서의 역할 |
|---|---|---|---|
| **원본 층** (현행) | 문서 1개 = `d/<slug>.html` | 사람 (docs/) | 근거. 그대로 보존, 링크로 도달 가능 |
| **토픽 층** (신설) | 주제 1개 = `t/<topic>.html` | AI 가 초안, 사람이 수정 가능 | **위키의 얼굴.** 홈·카테고리·검색은 토픽을 먼저 보여준다 |

토픽 하나는 원본 1개 이상을 **근거로 삼아** AI 가 새로 쓴 정돈된 문서다.
원본은 합쳐지지도 지워지지도 않는다. 자리만 "본문"에서 "근거"로 내려간다.
이렇게 하면 규칙 1(원본 불변), 규칙 5(병합·은닉 금지), 새 요구(정돈된 최신 정보)가 충돌하지 않는다.

> **CLAUDE.md 규칙 5 문구 수정 제안** (사용자 승인 필요):
> "문서를 병합하거나 감추지 않는다" → "**원본 문서 페이지**를 병합하거나 감추지 않는다.
> 정돈된 내용은 토픽 페이지에 따로 쓰고, 원본 페이지는 근거로 남긴다."

### 8.3 토픽은 어떻게 정해지는가

카테고리(`taxonomy.yaml`)는 사람이 소유하지만, **토픽은 AI 가 만든다.**
토픽까지 승인제로 두면 "파일을 넣는 것 외에 할 일이 없어야 한다"는 목표가 깨진다.
카테고리는 위키의 뼈대라 무한 증식이 치명적이지만, 토픽은 카테고리 안의 묶음이라
잘못 나뉘어도 `/wiki-audit` 에서 합치자고 제안하면 된다.

분류 단계에서 AI 는 문서마다 `topic` 을 정한다:

1. 기존 토픽 중 같은 주제가 있으면 → 그 토픽에 합류 (`sources` 에 추가)
2. 없으면 → 새 토픽 생성. id 는 영문 소문자-하이픈, 카테고리 안에서 고유
3. 판단 기준은 "한 사람이 이 주제를 배우려 할 때 한 페이지에서 읽고 싶은 범위".
   너무 넓으면(문서 6개 이상) audit 이 분할을 제안하고, 너무 좁으면(1개짜리가 오래 남음) 그대로 둔다

현재 10개 문서로 시뮬레이션하면 이렇게 나뉜다 (실제 분류는 AI 가 다시 한다):

| 토픽 (안) | 근거 문서 | AI 합성 |
|---|---|---|
| Git 시작하기와 멀티 PC 동기화 | git-guide, git-methods-comparison | ○ (겹침 medium) |
| Git 되돌리기와 갈라짐(diverge) 복구 | git-revert-guide, git-diverge-guide | ○ |
| AI 코딩 도구로 웹 앱 개발·배포하기 | dev-workflow-guide, webapp-development-workflow-guide | ○ (겹침 medium) |
| Bash 입문 | bash-guide | × 단일 원본 |
| uv 로 Python 환경 관리 | uv-환경-guidebook | × 단일 원본 |
| 혼동 행렬과 분류 평가 지표 | confusion-matrix-guide | × 단일 원본 |
| Claude 제품 관계도 | claude-제품-관계도 | × 단일 원본 |

### 8.4 언제 AI 가 본문을 쓰는가 (비용 통제)

**근거가 2개 이상이거나, 근거 중 하나라도 `outdated` 표시가 있을 때만** AI 가 합성한다.
근거가 1개뿐이고 문제도 없는 토픽은 원본 추출본을 그대로 토픽 본문으로 쓴다(AI 호출 없음).
문서 하나를 넣을 때마다 위키 전체를 다시 쓰는 낭비를 막고, 원본이 하나뿐일 때
"정돈"이라는 이름으로 내용이 바뀌는 일도 막는다.

재합성 여부는 지금의 `classifier_hash` 와 같은 방식으로 판단한다:

```
synth_hash = sha256( 근거 문서들의 source_hash 를 정렬해 이어 붙인 것
                   + synthesize.md 프롬프트 파일의 해시 )
```

근거 문서가 하나라도 바뀌거나 합성 지침이 바뀌면 다시 쓴다. 아니면 건드리지 않는다.

### 8.5 데이터 모델

```
wiki/topics/<topic-id>.json     메타데이터 (AI + 사람)
wiki/topics/<topic-id>.md       정돈된 본문 (AI 초안, 사람 수정 가능)
wiki/entries/<slug>.json        topic 필드 추가
```

```jsonc
// wiki/topics/git-getting-started.json
{
  "id": "git-getting-started",
  "title": "Git 시작하기와 멀티 PC 동기화",
  "category": "version-control",
  "subcategory": "basics",
  "summary": "…",                         // 목록·검색에 노출
  "tags": ["git", "github", "clone"],
  "sources": ["git-guide", "git-methods-comparison"],
  "primary": "git-guide",                 // 충돌 시 기준으로 삼은 문서
  "mode": "synthesized",                  // synthesized | mirror(단일 원본 그대로)
  "synth_hash": "…",
  "synthesized_at": "2026-09-24",
  "synthesized_by": "claude-code",        // claude-code | api | manual
  "locked": false,                        // true 면 AI 가 다시 쓰지 않는다
  "notes": ""                             // 사람 전용 (덮어쓰지 않음)
}
```

본문을 **Markdown** 으로 두는 이유: AI 가 자연스럽게 쓰고, 사람이 고치기 쉽고, git diff 가 읽힌다.
빌드 시 `markdown-it-py` 로 HTML 로 바꾼다 (의존성 1개 추가).

### 8.6 합성 지침 (`pipeline/prompts/synthesize.md`) 의 핵심 규칙

분류 지침과 마찬가지로 **파일 하나**를 슬래시 커맨드와 API 가 공유한다. 담길 규칙:

1. **원문에 없는 내용을 만들지 않는다.** 정돈은 "합치고 줄이고 순서를 잡는 것"이지 새로 쓰는 것이 아니다.
2. **충돌하면 최신을 따르되 기록한다.** 두 원본이 어긋나면 `doc_date` 가 늦은 쪽(또는 `supersedes` 쪽)을
   본문에 채택하고, 채택하지 않은 쪽을 **"원문과 다른 점"** 절에 표로 남긴다 (무엇을 / 어느 문서 대신 / 왜).
3. **명백한 오류만 고치고, 고쳤으면 표시한다.** 구식 버전·폐기된 명령처럼 확실한 것만 바로잡고
   같은 절에 기록한다. 애매하면 원문대로 두고 `⚠` 로 표시한다. 독자가 AI 의 손길을 항상 볼 수 있어야 한다.
4. **절마다 근거를 단다.** 각 절 끝에 `출처: git-guide §최초 세팅` 형식으로. 빌드가 이를 원본 페이지 앵커 링크로 바꾼다.
5. **다이어그램과 긴 코드는 옮기지 않고 가리킨다.** SVG 는 원본 페이지 링크로, 코드는 핵심만 싣고 전체는 링크.
6. **분량은 근거 합계보다 짧게.** 중복을 걷어냈다면 당연히 짧아져야 한다.
7. 고정 골격: `한 줄 요약 → 핵심 정리 → 본문(절) → 원문과 다른 점 → 근거 문서`.

### 8.7 사람이 고친 본문을 지키는 법

AI 가 재합성하면 `.md` 전체가 새로 써진다. 사람의 수정을 잃지 않도록:

- `locked: true` — AI 는 이 토픽을 다시 쓰지 않는다. 근거가 바뀌면 audit 리포트에
  "잠긴 토픽의 근거가 바뀜" 으로만 알린다. 사람이 풀거나 직접 고친다.
- `.md` 안의 `<!-- memo -->` … `<!-- /memo -->` 블록은 재합성 시 그대로 옮겨 붙인다. (`notes` 의 본문판)

### 8.8 사이트 변화

| 페이지 | 지금 | 바뀐 뒤 |
|---|---|---|
| 홈 | 카테고리 → 최근 **문서** | 카테고리 → 최근 **토픽** |
| 카테고리 | 문서 목록 | **토픽 목록**, 각 토픽 아래 근거 문서를 접어서 표시 |
| 토픽 `t/<id>.html` | 없음 | 정돈된 본문 + 목차 + "원문과 다른 점" + 근거 문서 목록(원본 보기 링크 포함) |
| 문서 `d/<slug>.html` | 본문 | 그대로. 상단에 "이 문서는 토픽 〈…〉 에 정리되어 있습니다 → 정돈된 최신 내용 보기" 배너 |
| 검색 | 문서만 | 토픽을 먼저, 문서를 뒤에 |

`mode: mirror` 인 토픽(단일 원본)은 원본 추출본을 본문으로 쓰되 토픽 페이지로 렌더링해
독자는 토픽/문서 구분을 신경 쓰지 않아도 된다.

### 8.9 바뀐 동기화 흐름

```
docs/*.html
    ▼
wiki scan          추출 + entry 골격                              (기계)
    ▼
분류               category/tags/summary + ★topic 배정            (AI)
    ▼
★합성              synth_hash 가 바뀐 토픽만 topics/<id>.md 작성   (AI, 근거 ≥ 2 또는 outdated 일 때만)
    ▼
wiki audit         + 재합성 필요 토픽 / 잠긴 토픽의 근거 변경 / 근거 6개 이상 토픽 분할 제안
    ▼
wiki build         + t/<id>.html, 문서 배너, 토픽 우선 검색 색인
```

`/wiki-sync` 에 3단계(합성)가 들어간다. 사용자가 하는 일은 여전히 파일을 넣고 `/wiki-sync` 한 번.

### 8.10 신뢰 장치 (audit 에 추가되는 검사)

- **인용 검사**: 토픽의 모든 근거 문서가 본문에서 최소 한 번 `출처:` 로 인용됐는가. 아니면 그 근거는 버려진 것.
- **코드 검사**: 본문의 코드블록이 근거 문서 어딘가에 (공백 무시) 존재하는가. 없으면 "원문과 다른 점"에
  기록되어 있어야 한다. 기록 없으면 리포트에 "출처 불명 코드" 로 뜬다.
- **분량 검사**: 본문 글자 수가 근거 합계를 넘으면 경고 (정돈이 아니라 부풀림).

### 8.11 범위 (Phase 5)

- **5a 골격**: 토픽 데이터 모델, `topic` 필드, 합성 지침, `/wiki-sync` 3단계, 토픽 페이지 빌드, 문서 배너.
  현재 10개 문서로 토픽 7개(합성 3 + mirror 4)를 만들어 검증.
- **5b 신뢰**: audit 검사 3종, `locked`/memo 보존, 토픽 우선 검색.
- **5c 무인화**: `uv run wiki synthesize` (API 경로). Phase 3 와 함께.

### 8.12 위험

| 위험 | 대응 |
|---|---|
| AI 가 정돈하면서 내용을 잃거나 바꾼다 | 원본 페이지 상시 노출 + "원문과 다른 점" 강제 + 인용·코드 검사 |
| 토픽이 어정쩡하게 나뉜다 | audit 이 분할·병합 제안, 사람이 `sources` 를 옮기면 다음 sync 에 재합성 |
| 재합성이 사람 수정을 지운다 | `locked`, `<!-- memo -->` 블록 |
| AI 호출 비용 | 근거 2개 이상일 때만, 해시로 변경분만 |
| Markdown 이 원본의 표·다이어그램을 못 담는다 | 표는 Markdown 표로, 다이어그램은 원본 링크. 손실은 원본 페이지가 메운다 |

### 8.13 사용자가 결정할 것

1. 8.2 의 **CLAUDE.md 규칙 5 문구 수정** 승인
2. 8.3 의 **토픽 자동 생성** (승인제 아님) 동의 여부
3. 8.4 의 **단일 원본 토픽은 AI 합성 생략** 동의 여부 (모두 합성하면 품질은 균일해지나 비용·변형 위험 증가)
4. 착수 시점 — 승인되면 5a 부터 구현한다
