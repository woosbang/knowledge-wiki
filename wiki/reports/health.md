# 위키 건강검진 리포트

생성 시각: 2026-09-24T00:49:59 · 문서 10개

> 이 파일은 `uv run wiki audit` 가 자동 생성한다. 직접 고치지 말 것.
> 조치는 `wiki/entries/<slug>.json` 또는 `wiki/taxonomy.yaml` 을 수정해서 한다.

## 1. 분류가 필요한 문서

없음. 모든 문서가 분류되어 있다.

## 2. 중복 의심 쌍

없음.

## 3. 오래된 문서 (점검 권장)

없음.

## 4. outdated / superseded 표시된 문서

없음.

## 5. 카테고리 현황

- `version-control`: 4개
- `dev-environment`: 2개
- `ai-ml`: 2개
- `dev-workflow`: 2개
